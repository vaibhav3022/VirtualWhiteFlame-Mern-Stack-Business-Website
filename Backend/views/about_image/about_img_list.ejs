<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>About Image List</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

<nav class="navbar navbar-dark bg-dark">
  <div class="container-fluid">
    <a href="/about_img" class="navbar-brand">⬅ Back</a>
    <a href="/add_about_img" class="navbar-brand">+ Add Image</a>
  </div>
</nav>

<div class="container mt-5">
  <h3 class="mb-4 fw-bold">About Images</h3>

  <table class="table table-bordered table-striped">
    <thead class="table-dark">
      <tr>
        <th>ID</th>
        <th>Image</th>
        <th>Action</th>
      </tr>
    </thead>

    <tbody>
      <% result.forEach(item => { %>
        <tr>
          <td><%= item.id %></td>
          <td><img src="/<%= item.about_img %>" width="100"></td>
          <td>
            <a href="/edit_about_img/<%= item.id %>" class="btn btn-warning btn-sm">Edit</a>
            <a href="/delete_about_img/<%= item.id %>" class="btn btn-danger btn-sm">Delete</a>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

</body>
</html>
